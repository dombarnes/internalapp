<h1>Users</h1>
<ul>
<p id="notice"><%= notice %></p>
<table class="index">
  <tr>
    <th>Email</th>
    <th>Comany</th>
    <th>Job Title</th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= user.company_name %></td>
    <td><%= user.job_title %></td>
    <td><%= link_to 'Show', user %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
  </tr>
<% end %>
</table>

<br />
<h2>There <%= pluralize(@users.length, 'user')%></h2>
<%= link_to 'New User', new_user_path %>
