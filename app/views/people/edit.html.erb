<h1>Editing <%= @person.first_name + " " + @person.last_name %></h1>

<%= render 'form' %>
</ br>
<div id="bottom_nav">
  <div id="back_button">
    <%= link_to 'Back', people_path, :class => "navigation_link" %>
  </div>
</div>