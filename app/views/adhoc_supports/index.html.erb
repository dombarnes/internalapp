<% content_for :sidebar do %>
  <h3>Ad Hoc Sidebar</h3>
	<p>There will be some content here. Probably a list of last edited content.
<table>
  <% @adhoc_supports.each do |adhoc_support| %>
    <tr>
    <td><%= adhoc_support.date %> - <%= adhoc_support.client_id %></td>
    </tr>
    <% end %>
    </table>
<% end %>


<h1>Ad Hoc Support</h1>
<p>Below is a list of ad-hoc support days provided.
<p>
<%= link_to 'New Adhoc support', new_adhoc_support_path %>
<table>
  <tr>
    <th>Date</th>
    <th>Type</th>
    <th>Technicians</th>
    <th>Client</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @adhoc_supports.each do |adhoc_support| %>
  <tr>
    <td><%= adhoc_support.date %></td>
    <td><%= adhoc_support.job_type %></td>
    <td><%= adhoc_support.technicians %></td>
    <td><%= adhoc_support.client_id %></td>
    <td><%= link_to 'Show', adhoc_support %></td>
    <td><%= link_to 'Edit', edit_adhoc_support_path(adhoc_support) %></td>
    <td><%= link_to 'Destroy', adhoc_support, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />
