<%= form_for(@adhoc_support) do |f| %>
  <% if @adhoc_support.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@adhoc_support.errors.count, "error") %> prohibited this adhoc_support from being saved:</h2>

      <ul>
      <% @adhoc_support.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
	<%= f.text_field :date %>
  </div>

  <div class="field">
    <%= f.label :job_type %><br />
	<select id="adhoc_supports_job_type" name="adhoc_support[job_type]">
		<option value="" selected="selected"></option>
    	<option value="Full Day" selected="selected" >Full Day</option>
		<option value="Half Day" selected="selected" >Half Day</option>
	</select>
  </div>

  <div class="field">
    <%= f.label :technicians %><br />
	<%= f.text_field :technicians %>
  </div>
<div>
	<%= f.label "Client" %>
<select id="adhoc_supports_client_id" name="adhoc_support[client_id]">
	<% if @adhoc_support.client_id == 0 or @adhoc_support.client_id == nil %>
    <option value="" selected="selected" ></option>
	<% end -%>
    <% @item = Company.find(:all, :order => "company_name")
	@item.each do |item| %>
  <option value="<%= item.company_name %>"
   <% if @adhoc_support.client_id == item.id -%> selected="selected"<% end %> >
 <%= item.company_name %>
 </option>
 <% end %>
</select>
</div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div>
  <a class="button" href="#" onclick="$('#user_form).submit(); return false;"><span>Submit</span></a>
  </div>
<% end %>
